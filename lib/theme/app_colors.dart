import 'package:flutter/material.dart';

/// Central palette for AgriConnect, derived from the brand mark.
class AppColors {
  AppColors._();

  static const Color leaf = Color(0xFF66BB6A); // light peak
  static const Color forest = Color(0xFF1B5E20); // dark peak
  static const Color primary = Color(0xFF2E7D32); // core brand green
  static const Color primaryDark = Color(0xFF1F5622);
  static const Color accent = Color(0xFF43A047); // mid green

  static const Color background = Color(0xFFF7FAF5);
  static const Color heroFade = Color(0xFFDCEFDC); // green used in the hero photo fade
  static const Color surface = Colors.white;
  static const Color fieldFill = Color(0xFFF1F5EE);
  static const Color border = Color(0xFFE3EAE0);

  static const Color textPrimary = Color(0xFF1A2A1C);
  static const Color textSecondary = Color(0xFF74857A);
  static const Color textMuted = Color(0xFFA3B0A0);
}